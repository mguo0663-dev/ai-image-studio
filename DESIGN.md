# DESIGN.md

## 气质与意象
午夜高端摄影工作室的控制台——深色空间中仅有屏幕微光与按钮呼吸灯，一切操作安静且精准。用户置身其中，像是一位在暗房中审阅作品的摄影师。

## 配色方案
- **主背景**: `#09090b`（近纯黑，zinc-950），像深夜关灯后的摄影暗房
- **卡片/面板背景**: `#18181b`（zinc-900），微微亮起的控制面板
- **边框**: `#27272a`（zinc-800），极细的分割线
- **主强调色**: `#a1a1aa`（zinc-400），冷银灰——低调但高级
- **次强调色**: `#f4f4f5`（zinc-100），文字与重点信息
- **状态色-生成中**: `#60a5fa`（蓝色微光，呼吸动画）
- **状态色-成功**: `#4ade80`（极淡绿，不喧哗）
- **状态色-错误**: `#f87171`（克制红）

## 字体排版
- 中文: `"Noto Sans SC"`, 无衬线, Google Fonts
- 英文/数字: `"Inter"`, 等宽数字
- 标题: 18-20px, font-weight 600, tracking tight
- 正文: 14px, font-weight 400
- 辅助说明: 12px, font-weight 400, zinc-500

## 页面结构
- **顶部**: 极简标题栏（Logo + 历史记录按钮）
- **主区域**: 画廊墙——等高矩形卡片按时间倒序排列，生成的图像与上传的参考图混排
  - 生成中的卡片呈呼吸模糊噪波加载态
  - 卡片悬停显示操作浮层（放大/下载/分享/复用）
- **底部**: 统一圆角矩形输入卡片（`rounded-2xl`, `bg-zinc-900/95`, 毛玻璃 + 阴影，悬浮于画廊上方）
  - **上区**: 参考图缩略图 + 添加按钮 | 风格标签（可拖拽排序/删除），仅在有内容时显示
  - **中区**: 左侧上传按钮（无参考图时显示）+ 文本输入区（无边框，融入卡片）+ 右侧圆形生成按钮（白色圆形 ArrowUp / 蓝色呼吸加载）
  - **下区**: 参数 pill 按钮行（风格下拉 / 比例下拉 / 数量切换 / 模型下拉），紧凑横排
- **历史侧栏**: 点击标题栏按钮弹出覆盖层，半透明遮罩

## 动效与交互
- 生成按钮: 圆形白色按钮（ArrowUp），加载时蓝色呼吸脉冲动画
- 参数控件: 紧凑 pill 下拉按钮，选中高亮，向上弹出选项面板
- 画廊卡片悬停: 微微放大 + 边框亮度提升（zinc-800 → zinc-600）
- 参考图拖拽: 半透明跟随 + 目标位置高亮
- 图片生成中: 呼吸模糊噪波动画（渐变光斑脉动 + 蓝色微光滑动 + 边框微光呼吸）
- 图片生成完成: 淡入 + 轻微缩放（scale 0.92→1, opacity 0→1）
- 弹窗/放大: 居中渐显 + 背景模糊
- 错误提示: 底部栏上方横幅，可关闭

## 设计禁忌
- 禁止大面积亮色/渐变/彩色背景
- 禁止圆角大于 16px（输入卡片可用 2xl=16px，内部控件 8-12px）
- 禁止使用 emoji 或装饰性图标
- 禁止阴影过重（仅用极淡的 zinc-950/50 阴影）
- 禁止动画时间 >300ms（保持响应感）
